node_modules/
dist/
*.html
.env
.env.local

# generated by `timeaudit <url>`
source-cache/

# Firebase Admin SDK service-account keys (db.js push/delete) — never commit.
# Keep the key file outside the repo entirely if you can; this is a backstop.
*firebase-adminsdk*.json
service-account*.json

